# Ko Ko's Birthday Page

A single self-contained HTML page (candle-blow interaction → birthday message + photo gallery).
No build step, no dependencies — deploys as a static site.

## Deploy

Connected to Vercel: push to `main` and it deploys automatically.
